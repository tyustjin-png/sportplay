# FitPet Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建 FitPet —— 一个通过摄像头检测运动动作、以桌面宠物成长激励坚持锻炼的 macOS Electron 应用

**Architecture:** Electron 双窗口（运动主窗口 + 常驻悬浮宠物窗口），渲染进程中用 MediaPipe WASM 做姿态检测，SQLite 本地存储，纯函数实现成长逻辑。

**Tech Stack:** Electron, @mediapipe/tasks-vision, better-sqlite3, Vitest (测试)

---

## Task 1: 项目骨架初始化

**Files:** Create `fitpet/package.json`, `fitpet/main.js`, `fitpet/preload.js`, `fitpet/src/renderer/workout/index.html`, `fitpet/src/renderer/pet-overlay/index.html`

- [ ] 1.1 创建 `fitpet/package.json`

```json
{
  "name": "fitpet",
  "version": "0.1.0",
  "description": "运动检测 + 桌面宠物成长系统",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14",
    "better-sqlite3": "^11.0.0"
  }
}
```

- [ ] 1.2 创建 `fitpet/main.js`（Electron 主进程，双窗口）

```js
// main.js — Electron 主进程：创建运动主窗口 + 宠物悬浮窗
const { app, BrowserWindow, ipcMain, screen } = require('electron');
const path = require('path');

let workoutWindow = null;
let petWindow = null;

function createPetWindow() {
  // 获取屏幕尺寸，将悬浮窗放在右下角
  const { width, height } = screen.getPrimaryDisplay().workAreaSize;

  petWindow = new BrowserWindow({
    width: 200,
    height: 240,
    x: width - 220,
    y: height - 260,
    alwaysOnTop: true,
    transparent: true,
    frame: false,
    resizable: false,
    skipTaskbar: true,
    hasShadow: false,
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      contextIsolation: true,
      nodeIntegration: false,
    },
  });

  petWindow.loadFile('src/renderer/pet-overlay/index.html');

  // 非交互区域鼠标穿透
  petWindow.setIgnoreMouseEvents(true, { forward: true });

  petWindow.on('closed', () => { petWindow = null; });
}

function createWorkoutWindow() {
  workoutWindow = new BrowserWindow({
    width: 960,
    height: 720,
    show: false,
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      contextIsolation: true,
      nodeIntegration: false,
    },
  });

  workoutWindow.loadFile('src/renderer/workout/index.html');
  workoutWindow.on('closed', () => { workoutWindow = null; });
}

app.whenReady().then(() => {
  createPetWindow();
  createWorkoutWindow();
});

// IPC：悬浮窗或快捷键触发显示运动窗口
ipcMain.on('show-workout', () => {
  if (!workoutWindow) createWorkoutWindow();
  workoutWindow.show();
  workoutWindow.focus();
});

// IPC：运动完成后更新宠物状态
ipcMain.on('workout-completed', (_event, data) => {
  if (petWindow) {
    petWindow.webContents.send('pet-update', data);
  }
});

// IPC：悬浮窗请求切换鼠标穿透
ipcMain.on('set-ignore-mouse', (_event, ignore) => {
  if (petWindow) {
    petWindow.setIgnoreMouseEvents(ignore, { forward: true });
  }
});

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit();
});
```

- [ ] 1.3 创建 `fitpet/preload.js`

```js
// preload.js — contextBridge 暴露安全的 IPC 接口给渲染进程
const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('fitpet', {
  // 运动窗口 → 主进程
  showWorkout: () => ipcRenderer.send('show-workout'),
  workoutCompleted: (data) => ipcRenderer.send('workout-completed', data),

  // 悬浮窗 → 主进程
  setIgnoreMouse: (ignore) => ipcRenderer.send('set-ignore-mouse', ignore),

  // 主进程 → 悬浮窗
  onPetUpdate: (callback) => ipcRenderer.on('pet-update', (_e, data) => callback(data)),
});
```

- [ ] 1.4 创建最小 HTML 占位文件

`fitpet/src/renderer/workout/index.html`:
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head><meta charset="UTF-8"><title>FitPet 运动</title></head>
<body><h1>运动主窗口</h1><p>待实现</p></body>
</html>
```

`fitpet/src/renderer/pet-overlay/index.html`:
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head><meta charset="UTF-8"><title>FitPet 宠物</title></head>
<body style="margin:0;background:transparent;"><p style="color:white;">🐣</p></body>
</html>
```

- [ ] 1.5 安装依赖并验证启动

```bash
cd fitpet && npm install
```

期望输出：`added XX packages`，无 error。

```bash
npx electron . &
sleep 3 && kill %1
```

期望：两个窗口短暂出现（悬浮窗在右下角），进程正常退出。

---

## Task 2: 数据库 Schema

**Files:** Create `fitpet/src/db/schema.js`, Test `fitpet/tests/db/schema.test.js`

- [ ] 2.1 创建测试 `fitpet/tests/db/schema.test.js`

```js
// tests/db/schema.test.js — 数据库初始化测试
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import Database from 'better-sqlite3';
import { initDatabase } from '../../src/db/schema.js';

describe('initDatabase', () => {
  let db;

  beforeEach(() => {
    db = new Database(':memory:');
  });

  afterEach(() => {
    db.close();
  });

  it('应创建所有必要的表', () => {
    initDatabase(db);
    const tables = db.prepare(
      "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name"
    ).all().map(r => r.name);
    expect(tables).toContain('workout_plans');
    expect(tables).toContain('daily_sessions');
    expect(tables).toContain('pet_state');
    expect(tables).toContain('daily_summary');
  });

  it('应插入默认运动计划（5 项）', () => {
    initDatabase(db);
    const plans = db.prepare('SELECT * FROM workout_plans ORDER BY display_order').all();
    expect(plans).toHaveLength(5);
    expect(plans[0].exercise).toBe('pushup');
  });

  it('应初始化宠物状态为境界1级别1', () => {
    initDatabase(db);
    const pet = db.prepare('SELECT * FROM pet_state WHERE id = 1').get();
    expect(pet.current_realm).toBe(1);
    expect(pet.current_level).toBe(1);
    expect(pet.streak_days).toBe(0);
  });

  it('重复调用不应报错（IF NOT EXISTS）', () => {
    initDatabase(db);
    expect(() => initDatabase(db)).not.toThrow();
  });
});
```

- [ ] 2.2 运行测试，确认失败

```bash
cd fitpet && npx vitest run tests/db/schema.test.js
```

期望输出：`FAIL`（模块不存在）

- [ ] 2.3 实现 `fitpet/src/db/schema.js`

```js
// src/db/schema.js — 数据库初始化：建表 + 默认数据
import Database from 'better-sqlite3';

/**
 * 初始化数据库表结构和默认数据
 * @param {Database.Database} db - better-sqlite3 实例
 */
export function initDatabase(db) {
  // 运动计划配置表
  db.exec(`
    CREATE TABLE IF NOT EXISTS workout_plans (
      id INTEGER PRIMARY KEY,
      exercise TEXT NOT NULL,
      sets INTEGER,
      reps INTEGER,
      duration_seconds INTEGER,
      display_order INTEGER
    )
  `);

  // 每日运动记录表
  db.exec(`
    CREATE TABLE IF NOT EXISTS daily_sessions (
      id INTEGER PRIMARY KEY,
      date TEXT NOT NULL,
      exercise TEXT NOT NULL,
      completed_sets INTEGER,
      total_sets INTEGER,
      notes TEXT
    )
  `);

  // 宠物状态表（单行）
  db.exec(`
    CREATE TABLE IF NOT EXISTS pet_state (
      id INTEGER PRIMARY KEY DEFAULT 1,
      current_realm INTEGER DEFAULT 1,
      current_level INTEGER DEFAULT 1,
      streak_days INTEGER DEFAULT 0,
      last_active_date TEXT
    )
  `);

  // 每日汇总表
  db.exec(`
    CREATE TABLE IF NOT EXISTS daily_summary (
      date TEXT PRIMARY KEY,
      completion_rate REAL,
      streak_at_date INTEGER
    )
  `);

  // 插入默认运动计划（仅当表为空时）
  const count = db.prepare('SELECT COUNT(*) as c FROM workout_plans').get().c;
  if (count === 0) {
    const insert = db.prepare(
      'INSERT INTO workout_plans (exercise, sets, reps, duration_seconds, display_order) VALUES (?, ?, ?, ?, ?)'
    );
    const defaultPlans = [
      ['pushup',      3, 15, null, 1],   // 俯卧撑：3组×15个
      ['squat',       3, 20, null, 2],   // 深蹲：3组×20个
      ['zhan_zhuang', 1, null, 300, 3],  // 站桩：1组×5分钟
      ['situp',       3, 20, null, 4],   // 仰卧起坐：3组×20个
      ['kegel',       3, null, 10,  5],  // 凯格尔：3组×10秒
    ];
    const insertMany = db.transaction((plans) => {
      for (const p of plans) insert.run(...p);
    });
    insertMany(defaultPlans);
  }

  // 初始化宠物状态（仅当表为空时）
  const petCount = db.prepare('SELECT COUNT(*) as c FROM pet_state').get().c;
  if (petCount === 0) {
    db.prepare(
      'INSERT INTO pet_state (id, current_realm, current_level, streak_days) VALUES (1, 1, 1, 0)'
    ).run();
  }
}

/**
 * 创建并初始化一个数据库实例
 * @param {string} dbPath - 数据库文件路径
 * @returns {Database.Database}
 */
export function createDatabase(dbPath) {
  const db = new Database(dbPath);
  db.pragma('journal_mode = WAL');
  initDatabase(db);
  return db;
}
```

- [ ] 2.4 运行测试，确认通过

```bash
cd fitpet && npx vitest run tests/db/schema.test.js
```

期望输出：`4 passed`

---

## Task 3: DB Workout 查询层

**Files:** Create `fitpet/src/db/workout.js`, Test `fitpet/tests/db/workout.test.js`

- [ ] 3.1 创建测试 `fitpet/tests/db/workout.test.js`

```js
// tests/db/workout.test.js — 运动计划和会话记录的 CRUD 测试
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import Database from 'better-sqlite3';
import { initDatabase } from '../../src/db/schema.js';
import {
  getWorkoutPlans,
  saveSession,
  getSessionsByDate,
  getCompletionRate,
} from '../../src/db/workout.js';

describe('workout CRUD', () => {
  let db;

  beforeEach(() => {
    db = new Database(':memory:');
    initDatabase(db);
  });

  afterEach(() => { db.close(); });

  it('getWorkoutPlans 应返回5项默认计划', () => {
    const plans = getWorkoutPlans(db);
    expect(plans).toHaveLength(5);
    expect(plans[0].exercise).toBe('pushup');
    expect(plans[0].sets).toBe(3);
  });

  it('saveSession 应写入记录并返回 id', () => {
    const id = saveSession(db, {
      date: '2026-04-02',
      exercise: 'pushup',
      completed_sets: 3,
      total_sets: 3,
    });
    expect(id).toBeGreaterThan(0);
  });

  it('getSessionsByDate 应返回指定日期的所有记录', () => {
    saveSession(db, { date: '2026-04-02', exercise: 'pushup', completed_sets: 3, total_sets: 3 });
    saveSession(db, { date: '2026-04-02', exercise: 'squat', completed_sets: 2, total_sets: 3 });
    saveSession(db, { date: '2026-04-01', exercise: 'pushup', completed_sets: 1, total_sets: 3 });
    const sessions = getSessionsByDate(db, '2026-04-02');
    expect(sessions).toHaveLength(2);
  });

  it('getCompletionRate 应计算每日完成率（已完成组数/总组数）', () => {
    saveSession(db, { date: '2026-04-02', exercise: 'pushup', completed_sets: 3, total_sets: 3 });
    saveSession(db, { date: '2026-04-02', exercise: 'squat', completed_sets: 1, total_sets: 3 });
    // 完成率 = (3+1) / (3+3) = 4/6 ≈ 0.667
    const rate = getCompletionRate(db, '2026-04-02');
    expect(rate).toBeCloseTo(0.667, 2);
  });

  it('getCompletionRate 无记录时返回 0', () => {
    const rate = getCompletionRate(db, '2026-04-02');
    expect(rate).toBe(0);
  });
});
```

- [ ] 3.2 运行测试确认失败

```bash
cd fitpet && npx vitest run tests/db/workout.test.js
```

期望输出：`FAIL`

- [ ] 3.3 实现 `fitpet/src/db/workout.js`

```js
// src/db/workout.js — 运动计划和每日会话的 CRUD 操作

/**
 * 获取所有运动计划，按 display_order 排序
 * @param {import('better-sqlite3').Database} db
 * @returns {Array}
 */
export function getWorkoutPlans(db) {
  return db.prepare('SELECT * FROM workout_plans ORDER BY display_order').all();
}

/**
 * 保存一次运动会话记录
 * @param {import('better-sqlite3').Database} db
 * @param {{ date: string, exercise: string, completed_sets: number, total_sets: number, notes?: string }} session
 * @returns {number} 新记录 ID
 */
export function saveSession(db, session) {
  const result = db.prepare(
    'INSERT INTO daily_sessions (date, exercise, completed_sets, total_sets, notes) VALUES (?, ?, ?, ?, ?)'
  ).run(session.date, session.exercise, session.completed_sets, session.total_sets, session.notes || null);
  return result.lastInsertRowid;
}

/**
 * 获取指定日期的所有运动记录
 * @param {import('better-sqlite3').Database} db
 * @param {string} date - 'YYYY-MM-DD'
 * @returns {Array}
 */
export function getSessionsByDate(db, date) {
  return db.prepare('SELECT * FROM daily_sessions WHERE date = ?').all(date);
}

/**
 * 计算指定日期的完成率（已完成组数 / 总组数）
 * @param {import('better-sqlite3').Database} db
 * @param {string} date - 'YYYY-MM-DD'
 * @returns {number} 0.0 ~ 1.0
 */
export function getCompletionRate(db, date) {
  const row = db.prepare(
    'SELECT COALESCE(SUM(completed_sets), 0) as done, COALESCE(SUM(total_sets), 0) as total FROM daily_sessions WHERE date = ?'
  ).get(date);
  if (row.total === 0) return 0;
  return row.done / row.total;
}
```

- [ ] 3.4 运行测试确认通过

```bash
cd fitpet && npx vitest run tests/db/workout.test.js
```

期望输出：`5 passed`

---

## Task 4: 宠物成长纯函数

**Files:** Create `fitpet/src/pet/growth.js`, Test `fitpet/tests/pet/growth.test.js`

- [ ] 4.1 创建测试 `fitpet/tests/pet/growth.test.js`

```js
// tests/pet/growth.test.js — 宠物成长逻辑测试
import { describe, it, expect } from 'vitest';
import {
  getRealmForLevel,
  getLevelInRealm,
  calculateNewLevel,
  getRealmInfo,
  getPetMood,
} from '../../src/pet/growth.js';

describe('境界计算', () => {
  it('级别1-27属于第一境界', () => {
    expect(getRealmForLevel(1)).toBe(1);
    expect(getRealmForLevel(27)).toBe(1);
  });

  it('级别28属于第二境界', () => {
    expect(getRealmForLevel(28)).toBe(2);
  });

  it('级别55属于第三境界', () => {
    expect(getRealmForLevel(55)).toBe(3);
  });

  it('getLevelInRealm 返回境界内的相对级别', () => {
    expect(getLevelInRealm(1)).toBe(1);
    expect(getLevelInRealm(27)).toBe(27);
    expect(getLevelInRealm(28)).toBe(1);
    expect(getLevelInRealm(30)).toBe(3);
  });
});

describe('级别浮动', () => {
  it('完成率 >= 80%：+1级', () => {
    const result = calculateNewLevel(1, 10, 0.85);
    expect(result.level).toBe(11);
    expect(result.realm).toBe(1);
  });

  it('完成率 50-79%：级别不变', () => {
    const result = calculateNewLevel(1, 10, 0.60);
    expect(result.level).toBe(10);
  });

  it('完成率 < 50%：-1级', () => {
    const result = calculateNewLevel(1, 10, 0.30);
    expect(result.level).toBe(9);
  });

  it('不能降到当前境界起始级以下', () => {
    const result = calculateNewLevel(1, 1, 0.10);
    expect(result.level).toBe(1);  // 不低于境界1起始级=1
  });

  it('不能超过当前境界上限（需要升境界条件才能突破）', () => {
    const result = calculateNewLevel(1, 27, 0.90);
    // 未满足升境界条件，停在27
    expect(result.level).toBe(27);
    expect(result.realm).toBe(1);
  });

  it('满足升境界条件时突破到下一境界', () => {
    // 连续27天 >= 80%
    const result = calculateNewLevel(1, 27, 0.90, 27);
    expect(result.level).toBe(28);
    expect(result.realm).toBe(2);
  });

  it('第二境界降级不低于28', () => {
    const result = calculateNewLevel(2, 28, 0.10);
    expect(result.level).toBe(28);
  });
});

describe('宠物状态反馈', () => {
  it('完成率 >= 80% 返回 active', () => {
    expect(getPetMood(0.85)).toBe('active');
  });

  it('完成率 30-79% 返回 normal', () => {
    expect(getPetMood(0.50)).toBe('normal');
  });

  it('完成率 < 30% 返回 tired', () => {
    expect(getPetMood(0.20)).toBe('tired');
  });
});

describe('getRealmInfo', () => {
  it('返回境界名称和描述', () => {
    const info = getRealmInfo(1);
    expect(info.name).toBeTruthy();
  });
});
```

- [ ] 4.2 运行测试确认失败

```bash
cd fitpet && npx vitest run tests/pet/growth.test.js
```

期望输出：`FAIL`

- [ ] 4.3 实现 `fitpet/src/pet/growth.js`

```js
// src/pet/growth.js — 宠物成长系统纯函数（无 DB 依赖）

const LEVELS_PER_REALM = 27;

// 境界信息配置
const REALM_INFO = [
  { name: '第一境界', desc: '小蛋 → 幼崽（逐渐破壳）' },
  { name: '第二境界', desc: '少年武者，有装备' },
  { name: '第三境界', desc: '全套盔甲，光环特效' },
];

/**
 * 根据总级别计算所在境界（从1开始）
 * @param {number} level - 总级别（>=1）
 * @returns {number} 境界编号
 */
export function getRealmForLevel(level) {
  return Math.ceil(level / LEVELS_PER_REALM);
}

/**
 * 获取在当前境界内的相对级别（1~27）
 * @param {number} level - 总级别
 * @returns {number}
 */
export function getLevelInRealm(level) {
  return ((level - 1) % LEVELS_PER_REALM) + 1;
}

/**
 * 根据完成率计算新的总级别
 * @param {number} currentRealm - 当前境界
 * @param {number} currentLevel - 当前总级别
 * @param {number} completionRate - 今日完成率 (0~1)
 * @param {number} [consecutiveHighDays=0] - 连续高完成率天数（用于判断升境界）
 * @returns {{ level: number, realm: number }}
 */
export function calculateNewLevel(currentRealm, currentLevel, completionRate, consecutiveHighDays = 0) {
  // 当前境界的级别范围
  const realmStart = (currentRealm - 1) * LEVELS_PER_REALM + 1;
  const realmEnd = currentRealm * LEVELS_PER_REALM;

  let newLevel = currentLevel;

  if (completionRate >= 0.8) {
    newLevel = currentLevel + 1;
  } else if (completionRate < 0.5) {
    newLevel = currentLevel - 1;
  }
  // 50-79%：不变

  // 不低于当前境界起始级
  if (newLevel < realmStart) {
    newLevel = realmStart;
  }

  // 如果到达境界上限
  if (newLevel > realmEnd) {
    // 检查是否满足升境界条件：连续27天 >= 80%
    if (consecutiveHighDays >= LEVELS_PER_REALM) {
      // 升境界
      return { level: realmEnd + 1, realm: currentRealm + 1 };
    }
    newLevel = realmEnd; // 卡在上限
  }

  return { level: newLevel, realm: getRealmForLevel(newLevel) };
}

/**
 * 获取境界信息
 * @param {number} realm - 境界编号（从1开始）
 * @returns {{ name: string, desc: string }}
 */
export function getRealmInfo(realm) {
  if (realm <= REALM_INFO.length) {
    return REALM_INFO[realm - 1];
  }
  return { name: `第${realm}境界`, desc: '未知领域，等待探索' };
}

/**
 * 根据完成率返回宠物心情状态
 * @param {number} completionRate - 0~1
 * @returns {'active' | 'normal' | 'tired'}
 */
export function getPetMood(completionRate) {
  if (completionRate >= 0.8) return 'active';
  if (completionRate >= 0.3) return 'normal';
  return 'tired';
}
```

- [ ] 4.4 运行测试确认通过

```bash
cd fitpet && npx vitest run tests/pet/growth.test.js
```

期望输出：`10 passed`（或具体数量，全部绿色）

---

## Task 5: DB Pet 查询层

**Files:** Create `fitpet/src/db/pet.js`, Test `fitpet/tests/db/pet.test.js`

- [ ] 5.1 创建测试 `fitpet/tests/db/pet.test.js`

```js
// tests/db/pet.test.js — 宠物状态数据库操作测试
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import Database from 'better-sqlite3';
import { initDatabase } from '../../src/db/schema.js';
import {
  getPetState,
  updatePetState,
  saveDailySummary,
  getRecentSummaries,
  getConsecutiveHighDays,
} from '../../src/db/pet.js';

describe('pet DB 操作', () => {
  let db;

  beforeEach(() => {
    db = new Database(':memory:');
    initDatabase(db);
  });

  afterEach(() => { db.close(); });

  it('getPetState 返回初始状态', () => {
    const state = getPetState(db);
    expect(state.current_realm).toBe(1);
    expect(state.current_level).toBe(1);
  });

  it('updatePetState 更新境界和级别', () => {
    updatePetState(db, { current_realm: 1, current_level: 5, streak_days: 3, last_active_date: '2026-04-02' });
    const state = getPetState(db);
    expect(state.current_level).toBe(5);
    expect(state.streak_days).toBe(3);
  });

  it('saveDailySummary 写入每日汇总', () => {
    saveDailySummary(db, '2026-04-02', 0.85, 5);
    const row = db.prepare('SELECT * FROM daily_summary WHERE date = ?').get('2026-04-02');
    expect(row.completion_rate).toBeCloseTo(0.85);
    expect(row.streak_at_date).toBe(5);
  });

  it('getRecentSummaries 返回最近N天的汇总', () => {
    saveDailySummary(db, '2026-04-01', 0.90, 1);
    saveDailySummary(db, '2026-04-02', 0.85, 2);
    const summaries = getRecentSummaries(db, 7);
    expect(summaries).toHaveLength(2);
    // 按日期降序
    expect(summaries[0].date).toBe('2026-04-02');
  });

  it('getConsecutiveHighDays 统计连续高完成率天数', () => {
    saveDailySummary(db, '2026-03-31', 0.90, 1);
    saveDailySummary(db, '2026-04-01', 0.85, 2);
    saveDailySummary(db, '2026-04-02', 0.50, 3); // 断了
    // 从最近一天往回数，最近一天是 0.50 < 0.8，所以连续高完成率为0
    expect(getConsecutiveHighDays(db, '2026-04-02')).toBe(0);
  });

  it('getConsecutiveHighDays 连续3天高完成率', () => {
    saveDailySummary(db, '2026-03-31', 0.80, 1);
    saveDailySummary(db, '2026-04-01', 0.85, 2);
    saveDailySummary(db, '2026-04-02', 0.90, 3);
    expect(getConsecutiveHighDays(db, '2026-04-02')).toBe(3);
  });
});
```

- [ ] 5.2 运行测试确认失败

```bash
cd fitpet && npx vitest run tests/db/pet.test.js
```

期望输出：`FAIL`

- [ ] 5.3 实现 `fitpet/src/db/pet.js`

```js
// src/db/pet.js — 宠物状态和每日汇总的数据库操作

/**
 * 获取宠物当前状态
 * @param {import('better-sqlite3').Database} db
 * @returns {{ current_realm: number, current_level: number, streak_days: number, last_active_date: string | null }}
 */
export function getPetState(db) {
  return db.prepare('SELECT * FROM pet_state WHERE id = 1').get();
}

/**
 * 更新宠物状态
 * @param {import('better-sqlite3').Database} db
 * @param {{ current_realm: number, current_level: number, streak_days: number, last_active_date: string }} state
 */
export function updatePetState(db, state) {
  db.prepare(
    'UPDATE pet_state SET current_realm = ?, current_level = ?, streak_days = ?, last_active_date = ? WHERE id = 1'
  ).run(state.current_realm, state.current_level, state.streak_days, state.last_active_date);
}

/**
 * 保存每日汇总
 * @param {import('better-sqlite3').Database} db
 * @param {string} date
 * @param {number} completionRate
 * @param {number} streakAtDate
 */
export function saveDailySummary(db, date, completionRate, streakAtDate) {
  db.prepare(
    'INSERT OR REPLACE INTO daily_summary (date, completion_rate, streak_at_date) VALUES (?, ?, ?)'
  ).run(date, completionRate, streakAtDate);
}

/**
 * 获取最近 N 天的每日汇总，按日期降序
 * @param {import('better-sqlite3').Database} db
 * @param {number} days
 * @returns {Array}
 */
export function getRecentSummaries(db, days) {
  return db.prepare(
    'SELECT * FROM daily_summary ORDER BY date DESC LIMIT ?'
  ).all(days);
}

/**
 * 从指定日期向前计算连续高完成率（>= 0.8）的天数
 * @param {import('better-sqlite3').Database} db
 * @param {string} fromDate - 'YYYY-MM-DD'
 * @returns {number}
 */
export function getConsecutiveHighDays(db, fromDate) {
  // 从最近日期往前取所有汇总，逐天检查
  const summaries = db.prepare(
    'SELECT date, completion_rate FROM daily_summary WHERE date <= ? ORDER BY date DESC'
  ).all(fromDate);

  let count = 0;
  for (const s of summaries) {
    if (s.completion_rate >= 0.8) {
      count++;
    } else {
      break;
    }
  }
  return count;
}
```

- [ ] 5.4 运行测试确认通过

```bash
cd fitpet && npx vitest run tests/db/pet.test.js
```

期望输出：`6 passed`

---

## Task 6: MediaPipe 姿态检测封装

**Files:** Create `fitpet/src/detection/pose-detector.js`

- [ ] 6.1 创建 `fitpet/src/detection/pose-detector.js`

```js
// src/detection/pose-detector.js — MediaPipe PoseLandmarker 封装
// 注意：此模块在 Electron 渲染进程中运行

import { PoseLandmarker, FilesetResolver } from '@mediapipe/tasks-vision';

let poseLandmarker = null;

/**
 * 初始化 MediaPipe PoseLandmarker
 * 加载 WASM 文件并创建检测器实例
 * @param {string} [wasmPath] - WASM 文件目录路径，默认使用 CDN
 * @returns {Promise<PoseLandmarker>}
 */
export async function initPoseDetector(wasmPath) {
  const vision = await FilesetResolver.forVisionTasks(
    wasmPath || 'https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@latest/wasm'
  );

  poseLandmarker = await PoseLandmarker.createFromOptions(vision, {
    baseOptions: {
      modelAssetPath: 'https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_lite/float16/1/pose_landmarker_lite.task',
      delegate: 'GPU',
    },
    runningMode: 'VIDEO',
    numPoses: 1,
  });

  return poseLandmarker;
}

/**
 * 对视频帧进行姿态检测
 * @param {HTMLVideoElement} video - 摄像头视频元素
 * @param {number} timestamp - 帧时间戳（毫秒）
 * @returns {Array | null} landmarks 数组，每个 landmark 包含 {x, y, z, visibility}
 */
export function detectPose(video, timestamp) {
  if (!poseLandmarker) return null;
  const result = poseLandmarker.detectForVideo(video, timestamp);
  if (result.landmarks && result.landmarks.length > 0) {
    return result.landmarks[0]; // 只取第一个人
  }
  return null;
}

/**
 * 计算三点形成的角度（度数）
 * 用于关节角度计算（如肘关节、膝关节）
 * @param {{ x: number, y: number }} a - 点A
 * @param {{ x: number, y: number }} b - 点B（顶点）
 * @param {{ x: number, y: number }} c - 点C
 * @returns {number} 角度（0-180）
 */
export function calcAngle(a, b, c) {
  const radians = Math.atan2(c.y - b.y, c.x - b.x) - Math.atan2(a.y - b.y, a.x - b.x);
  let angle = Math.abs(radians * 180 / Math.PI);
  if (angle > 180) angle = 360 - angle;
  return angle;
}
```

- [ ] 6.2 此模块不写单元测试（依赖 WASM 和摄像头），在 Task 11 集成测试中验证。`calcAngle` 会在检测器测试中间接覆盖。

---

## Task 7: 俯卧撑检测器

**Files:** Create `fitpet/src/detection/pushup.js`, Test `fitpet/tests/detection/pushup.test.js`

- [ ] 7.1 创建测试 `fitpet/tests/detection/pushup.test.js`

```js
// tests/detection/pushup.test.js — 俯卧撑计数器测试
import { describe, it, expect } from 'vitest';
import { createPushupCounter } from '../../src/detection/pushup.js';

// 辅助函数：生成模拟 landmarks（只需肩、肘、腕的坐标）
// MediaPipe Pose landmark 索引：11=左肩, 13=左肘, 15=左腕
function makeLandmarks(elbowAngle) {
  // 简化：用固定点模拟特定肘角
  const landmarks = new Array(33).fill({ x: 0, y: 0, z: 0, visibility: 0.9 });
  // 肩在上，腕在下，肘在中间
  // 通过调整肘的位置来改变角度
  const shoulderY = 0.3;
  const wristY = 0.7;
  const elbowX = 0.5;

  // 用角度反算肘的Y位置（简化模型）
  const rad = elbowAngle * Math.PI / 180;
  const elbowY = 0.5 + 0.2 * Math.cos(rad);

  landmarks[11] = { x: 0.5, y: shoulderY, z: 0, visibility: 0.9 }; // 左肩
  landmarks[13] = { x: elbowX, y: elbowY, z: 0, visibility: 0.9 }; // 左肘
  landmarks[15] = { x: 0.5, y: wristY, z: 0, visibility: 0.9 };    // 左腕

  // 右侧镜像
  landmarks[12] = { x: 0.5, y: shoulderY, z: 0, visibility: 0.9 }; // 右肩
  landmarks[14] = { x: elbowX, y: elbowY, z: 0, visibility: 0.9 }; // 右肘
  landmarks[16] = { x: 0.5, y: wristY, z: 0, visibility: 0.9 };    // 右腕

  return landmarks;
}

describe('PushupCounter', () => {
  it('初始状态计数为0', () => {
    const counter = createPushupCounter();
    expect(counter.getState().count).toBe(0);
    expect(counter.getState().phase).toBe('up');
  });

  it('肘角从大变小再变大算一次俯卧撑', () => {
    const counter = createPushupCounter();
    // 开始：肘伸直（角度大）
    counter.update(makeLandmarks(170));
    expect(counter.getState().phase).toBe('up');

    // 下去：肘弯曲（角度小）
    counter.update(makeLandmarks(70));
    expect(counter.getState().phase).toBe('down');

    // 上来：肘伸直
    counter.update(makeLandmarks(170));
    expect(counter.getState().phase).toBe('up');
    expect(counter.getState().count).toBe(1);
  });

  it('连续3个俯卧撑', () => {
    const counter = createPushupCounter();
    for (let i = 0; i < 3; i++) {
      counter.update(makeLandmarks(170));
      counter.update(makeLandmarks(70));
      counter.update(makeLandmarks(170));
    }
    expect(counter.getState().count).toBe(3);
  });

  it('reset 清零', () => {
    const counter = createPushupCounter();
    counter.update(makeLandmarks(170));
    counter.update(makeLandmarks(70));
    counter.update(makeLandmarks(170));
    counter.reset();
    expect(counter.getState().count).toBe(0);
  });
});
```

- [ ] 7.2 运行测试确认失败

```bash
cd fitpet && npx vitest run tests/detection/pushup.test.js
```

期望输出：`FAIL`

- [ ] 7.3 实现 `fitpet/src/detection/pushup.js`

```js
// src/detection/pushup.js — 俯卧撑计数器（状态机：up ↔ down）
import { calcAngle } from './pose-detector.js';

// MediaPipe Pose Landmark 索引
const LEFT_SHOULDER = 11;
const LEFT_ELBOW = 13;
const LEFT_WRIST = 15;
const RIGHT_SHOULDER = 12;
const RIGHT_ELBOW = 14;
const RIGHT_WRIST = 16;

// 阈值
const DOWN_THRESHOLD = 90;   // 肘角小于此值 → 到底
const UP_THRESHOLD = 160;    // 肘角大于此值 → 完成一次

/**
 * 创建俯卧撑计数器（工厂函数）
 * @returns {{ update: (landmarks: Array) => void, getState: () => { count: number, phase: string }, reset: () => void }}
 */
export function createPushupCounter() {
  let count = 0;
  let phase = 'up'; // 'up' | 'down'

  function update(landmarks) {
    if (!landmarks) return;

    // 取左右肘角的平均值
    const leftAngle = calcAngle(
      landmarks[LEFT_SHOULDER],
      landmarks[LEFT_ELBOW],
      landmarks[LEFT_WRIST]
    );
    const rightAngle = calcAngle(
      landmarks[RIGHT_SHOULDER],
      landmarks[RIGHT_ELBOW],
      landmarks[RIGHT_WRIST]
    );
    const avgAngle = (leftAngle + rightAngle) / 2;

    if (phase === 'up' && avgAngle < DOWN_THRESHOLD) {
      phase = 'down';
    } else if (phase === 'down' && avgAngle > UP_THRESHOLD) {
      phase = 'up';
      count++;
    }
  }

  function getState() {
    return { count, phase };
  }

  function reset() {
    count = 0;
    phase = 'up';
  }

  return { update, getState, reset };
}
```

- [ ] 7.4 运行测试确认通过

```bash
cd fitpet && npx vitest run tests/detection/pushup.test.js
```

期望输出：`4 passed`

---

## Task 8: 深蹲检测器

**Files:** Create `fitpet/src/detection/squat.js`, Test `fitpet/tests/detection/squat.test.js`

- [ ] 8.1 创建测试 `fitpet/tests/detection/squat.test.js`

```js
// tests/detection/squat.test.js — 深蹲计数器测试
import { describe, it, expect } from 'vitest';
import { createSquatCounter } from '../../src/detection/squat.js';

// 模拟 landmarks：控制膝关节角度
// 23=左髋, 25=左膝, 27=左踝, 24=右髋, 26=右膝, 28=右踝
function makeLandmarks(kneeAngle) {
  const landmarks = new Array(33).fill({ x: 0, y: 0, z: 0, visibility: 0.9 });
  const hipY = 0.3;
  const ankleY = 0.8;
  const rad = kneeAngle * Math.PI / 180;
  const kneeY = 0.55 + 0.25 * Math.cos(rad);

  landmarks[23] = { x: 0.4, y: hipY, z: 0, visibility: 0.9 };
  landmarks[25] = { x: 0.4, y: kneeY, z: 0, visibility: 0.9 };
  landmarks[27] = { x: 0.4, y: ankleY, z: 0, visibility: 0.9 };
  landmarks[24] = { x: 0.6, y: hipY, z: 0, visibility: 0.9 };
  landmarks[26] = { x: 0.6, y: kneeY, z: 0, visibility: 0.9 };
  landmarks[28] = { x: 0.6, y: ankleY, z: 0, visibility: 0.9 };

  return landmarks;
}

describe('SquatCounter', () => {
  it('初始计数为0', () => {
    const counter = createSquatCounter();
    expect(counter.getState().count).toBe(0);
  });

  it('膝角从大变小再变大算一次深蹲', () => {
    const counter = createSquatCounter();
    counter.update(makeLandmarks(170));
    counter.update(makeLandmarks(80));
    counter.update(makeLandmarks(170));
    expect(counter.getState().count).toBe(1);
  });

  it('连续3个深蹲', () => {
    const counter = createSquatCounter();
    for (let i = 0; i < 3; i++) {
      counter.update(makeLandmarks(170));
      counter.update(makeLandmarks(80));
      counter.update(makeLandmarks(170));
    }
    expect(counter.getState().count).toBe(3);
  });

  it('reset 清零', () => {
    const counter = createSquatCounter();
    counter.update(makeLandmarks(170));
    counter.update(makeLandmarks(80));
    counter.update(makeLandmarks(170));
    counter.reset();
    expect(counter.getState().count).toBe(0);
  });
});
```

- [ ] 8.2 运行测试确认失败

```bash
cd fitpet && npx vitest run tests/detection/squat.test.js
```

期望输出：`FAIL`

- [ ] 8.3 实现 `fitpet/src/detection/squat.js`

```js
// src/detection/squat.js — 深蹲计数器（状态机：standing ↔ squatting）
import { calcAngle } from './pose-detector.js';

const LEFT_HIP = 23;
const LEFT_KNEE = 25;
const LEFT_ANKLE = 27;
const RIGHT_HIP = 24;
const RIGHT_KNEE = 26;
const RIGHT_ANKLE = 28;

const DOWN_THRESHOLD = 100;  // 膝角小于此值 → 蹲到位
const UP_THRESHOLD = 160;    // 膝角大于此值 → 站起来

/**
 * 创建深蹲计数器
 */
export function createSquatCounter() {
  let count = 0;
  let phase = 'standing'; // 'standing' | 'squatting'

  function update(landmarks) {
    if (!landmarks) return;

    const leftAngle = calcAngle(
      landmarks[LEFT_HIP], landmarks[LEFT_KNEE], landmarks[LEFT_ANKLE]
    );
    const rightAngle = calcAngle(
      landmarks[RIGHT_HIP], landmarks[RIGHT_KNEE], landmarks[RIGHT_ANKLE]
    );
    const avgAngle = (leftAngle + rightAngle) / 2;

    if (phase === 'standing' && avgAngle < DOWN_THRESHOLD) {
      phase = 'squatting';
    } else if (phase === 'squatting' && avgAngle > UP_THRESHOLD) {
      phase = 'standing';
      count++;
    }
  }

  function getState() { return { count, phase }; }
  function reset() { count = 0; phase = 'standing'; }

  return { update, getState, reset };
}
```

- [ ] 8.4 运行测试确认通过

```bash
cd fitpet && npx vitest run tests/detection/squat.test.js
```

期望输出：`4 passed`

---

## Task 9: 仰卧起坐检测器

**Files:** Create `fitpet/src/detection/situp.js`, Test `fitpet/tests/detection/situp.test.js`

- [ ] 9.1 创建测试 `fitpet/tests/detection/situp.test.js`

```js
// tests/detection/situp.test.js — 仰卧起坐计数器测试
import { describe, it, expect } from 'vitest';
import { createSitupCounter } from '../../src/detection/situp.js';

// 模拟：通过肩和髋的相对位置控制躯干角度
// 11=左肩, 23=左髋, 25=左膝
function makeLandmarks(torsoAngle) {
  const landmarks = new Array(33).fill({ x: 0, y: 0, z: 0, visibility: 0.9 });
  const hipX = 0.5;
  const hipY = 0.6;
  // 躯干角度：0度=完全躺平（肩与髋同高），90度=完全坐起
  const rad = torsoAngle * Math.PI / 180;
  const shoulderX = hipX - 0.3 * Math.cos(rad);
  const shoulderY = hipY - 0.3 * Math.sin(rad);

  landmarks[11] = { x: shoulderX, y: shoulderY, z: 0, visibility: 0.9 }; // 左肩
  landmarks[12] = { x: shoulderX + 0.1, y: shoulderY, z: 0, visibility: 0.9 }; // 右肩
  landmarks[23] = { x: hipX, y: hipY, z: 0, visibility: 0.9 };    // 左髋
  landmarks[24] = { x: hipX + 0.1, y: hipY, z: 0, visibility: 0.9 }; // 右髋
  landmarks[25] = { x: 0.5, y: 0.8, z: 0, visibility: 0.9 };     // 左膝
  landmarks[26] = { x: 0.6, y: 0.8, z: 0, visibility: 0.9 };     // 右膝

  return landmarks;
}

describe('SitupCounter', () => {
  it('初始计数为0', () => {
    const counter = createSitupCounter();
    expect(counter.getState().count).toBe(0);
  });

  it('躯干从平躺到抬起再回落算一次', () => {
    const counter = createSitupCounter();
    counter.update(makeLandmarks(5));   // 躺平
    counter.update(makeLandmarks(45));  // 抬起
    counter.update(makeLandmarks(5));   // 回落
    expect(counter.getState().count).toBe(1);
  });

  it('连续3个仰卧起坐', () => {
    const counter = createSitupCounter();
    for (let i = 0; i < 3; i++) {
      counter.update(makeLandmarks(5));
      counter.update(makeLandmarks(45));
      counter.update(makeLandmarks(5));
    }
    expect(counter.getState().count).toBe(3);
  });

  it('reset 清零', () => {
    const counter = createSitupCounter();
    counter.update(makeLandmarks(5));
    counter.update(makeLandmarks(45));
    counter.update(makeLandmarks(5));
    counter.reset();
    expect(counter.getState().count).toBe(0);
  });
});
```

- [ ] 9.2 运行测试确认失败

```bash
cd fitpet && npx vitest run tests/detection/situp.test.js
```

期望输出：`FAIL`

- [ ] 9.3 实现 `fitpet/src/detection/situp.js`

```js
// src/detection/situp.js — 仰卧起坐计数器（检测躯干与水平面的夹角）

const LEFT_SHOULDER = 11;
const LEFT_HIP = 23;
const RIGHT_SHOULDER = 12;
const RIGHT_HIP = 24;

const UP_THRESHOLD = 30;   // 躯干角度大于30度 → 抬起
const DOWN_THRESHOLD = 15; // 躯干角度小于15度 → 躺回

/**
 * 计算躯干与水平方向的夹角
 * @param {Array} landmarks
 * @returns {number} 角度（度）
 */
function getTorsoAngle(landmarks) {
  // 取左右肩和髋的中点
  const shoulderMidX = (landmarks[LEFT_SHOULDER].x + landmarks[RIGHT_SHOULDER].x) / 2;
  const shoulderMidY = (landmarks[LEFT_SHOULDER].y + landmarks[RIGHT_SHOULDER].y) / 2;
  const hipMidX = (landmarks[LEFT_HIP].x + landmarks[RIGHT_HIP].x) / 2;
  const hipMidY = (landmarks[LEFT_HIP].y + landmarks[RIGHT_HIP].y) / 2;

  // 躯干向量与水平线的夹角
  const dx = shoulderMidX - hipMidX;
  const dy = shoulderMidY - hipMidY;
  // 注意：屏幕坐标Y轴向下，所以取 atan2(-dy, |dx|) 可能更直观
  // 但我们简化为：如果肩比髋高（y更小），说明在抬起
  const angle = Math.abs(Math.atan2(hipMidY - shoulderMidY, Math.abs(dx))) * 180 / Math.PI;
  return angle;
}

/**
 * 创建仰卧起坐计数器
 */
export function createSitupCounter() {
  let count = 0;
  let phase = 'down'; // 'down' | 'up'

  function update(landmarks) {
    if (!landmarks) return;

    const angle = getTorsoAngle(landmarks);

    if (phase === 'down' && angle > UP_THRESHOLD) {
      phase = 'up';
    } else if (phase === 'up' && angle < DOWN_THRESHOLD) {
      phase = 'down';
      count++;
    }
  }

  function getState() { return { count, phase }; }
  function reset() { count = 0; phase = 'down'; }

  return { update, getState, reset };
}
```

- [ ] 9.4 运行测试确认通过

```bash
cd fitpet && npx vitest run tests/detection/situp.test.js
```

期望输出：`4 passed`

---

## Task 10: 站桩计时器

**Files:** Create `fitpet/src/detection/zhan-zhuang.js`, Test `fitpet/tests/detection/zhan-zhuang.test.js`

- [ ] 10.1 创建测试 `fitpet/tests/detection/zhan-zhuang.test.js`

```js
// tests/detection/zhan-zhuang.test.js — 站桩计时器测试
import { describe, it, expect, vi } from 'vitest';
import { createZhanZhuangTimer } from '../../src/detection/zhan-zhuang.js';

// 模拟标准站桩姿势的 landmarks
// 关键：双膝微曲（膝角约140-160度），站直，双臂抬起
function makeStandingLandmarks(kneeAngle = 150) {
  const landmarks = new Array(33).fill({ x: 0, y: 0, z: 0, visibility: 0.9 });
  const hipY = 0.4;
  const ankleY = 0.8;
  const rad = kneeAngle * Math.PI / 180;
  const kneeY = 0.6 + 0.2 * Math.cos(rad);

  // 髋、膝、踝
  landmarks[23] = { x: 0.4, y: hipY, z: 0, visibility: 0.9 };
  landmarks[25] = { x: 0.4, y: kneeY, z: 0, visibility: 0.9 };
  landmarks[27] = { x: 0.4, y: ankleY, z: 0, visibility: 0.9 };
  landmarks[24] = { x: 0.6, y: hipY, z: 0, visibility: 0.9 };
  landmarks[26] = { x: 0.6, y: kneeY, z: 0, visibility: 0.9 };
  landmarks[28] = { x: 0.6, y: ankleY, z: 0, visibility: 0.9 };

  // 肩
  landmarks[11] = { x: 0.4, y: 0.25, z: 0, visibility: 0.9 };
  landmarks[12] = { x: 0.6, y: 0.25, z: 0, visibility: 0.9 };

  return landmarks;
}

describe('ZhanZhuangTimer', () => {
  beforeEach(() => {
    vi.useFakeTimers();
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  it('初始状态：未开始，时间为0', () => {
    const timer = createZhanZhuangTimer();
    expect(timer.getState().elapsed).toBe(0);
    expect(timer.getState().isHolding).toBe(false);
  });

  it('检测到标准姿势后开始计时', () => {
    const timer = createZhanZhuangTimer();
    const now = Date.now();

    timer.update(makeStandingLandmarks(150), now);
    expect(timer.getState().isHolding).toBe(true);

    // 3秒后再次更新
    timer.update(makeStandingLandmarks(150), now + 3000);
    expect(timer.getState().elapsed).toBe(3000);
  });

  it('姿势崩溃时暂停计时', () => {
    const timer = createZhanZhuangTimer();
    const now = Date.now();

    timer.update(makeStandingLandmarks(150), now);
    timer.update(makeStandingLandmarks(150), now + 3000);

    // 姿势崩溃（蹲太低）
    timer.update(makeStandingLandmarks(80), now + 4000);
    expect(timer.getState().isHolding).toBe(false);

    // 再过2秒，时间不应增加
    timer.update(makeStandingLandmarks(80), now + 6000);
    expect(timer.getState().elapsed).toBe(3000); // 仍然是3秒
  });

  it('姿势恢复后继续计时', () => {
    const timer = createZhanZhuangTimer();
    const now = Date.now();

    timer.update(makeStandingLandmarks(150), now);
    timer.update(makeStandingLandmarks(150), now + 3000);

    // 崩溃
    timer.update(makeStandingLandmarks(80), now + 4000);

    // 恢复
    timer.update(makeStandingLandmarks(150), now + 5000);
    timer.update(makeStandingLandmarks(150), now + 7000);
    expect(timer.getState().elapsed).toBe(5000); // 3秒 + 2秒
  });

  it('reset 清零', () => {
    const timer = createZhanZhuangTimer();
    const now = Date.now();
    timer.update(makeStandingLandmarks(150), now);
    timer.update(makeStandingLandmarks(150), now + 5000);
    timer.reset();
    expect(timer.getState().elapsed).toBe(0);
    expect(timer.getState().isHolding).toBe(false);
  });
});
```

- [ ] 10.2 运行测试确认失败

```bash
cd fitpet && npx vitest run tests/detection/zhan-zhuang.test.js
```

期望输出：`FAIL`

- [ ] 10.3 实现 `fitpet/src/detection/zhan-zhuang.js`

```js
// src/detection/zhan-zhuang.js — 站桩计时器（静态姿势检测 + 累计计时）
import { calcAngle } from './pose-detector.js';

const LEFT_HIP = 23;
const LEFT_KNEE = 25;
const LEFT_ANKLE = 27;
const RIGHT_HIP = 24;
const RIGHT_KNEE = 26;
const RIGHT_ANKLE = 28;

// 站桩有效膝角范围：微曲但不能蹲太低
const MIN_KNEE_ANGLE = 120;
const MAX_KNEE_ANGLE = 175;

/**
 * 检测是否处于有效站桩姿势
 * @param {Array} landmarks
 * @returns {boolean}
 */
function isValidStance(landmarks) {
  const leftAngle = calcAngle(
    landmarks[LEFT_HIP], landmarks[LEFT_KNEE], landmarks[LEFT_ANKLE]
  );
  const rightAngle = calcAngle(
    landmarks[RIGHT_HIP], landmarks[RIGHT_KNEE], landmarks[RIGHT_ANKLE]
  );
  const avgAngle = (leftAngle + rightAngle) / 2;
  return avgAngle >= MIN_KNEE_ANGLE && avgAngle <= MAX_KNEE_ANGLE;
}

/**
 * 创建站桩计时器
 */
export function createZhanZhuangTimer() {
  let totalElapsed = 0;  // 累计有效时间（毫秒）
  let isHolding = false;
  let holdStartTime = null;

  function update(landmarks, timestamp) {
    if (!landmarks) return;

    const valid = isValidStance(landmarks);

    if (valid && !isHolding) {
      // 开始计时
      isHolding = true;
      holdStartTime = timestamp;
    } else if (valid && isHolding) {
      // 继续计时（不做操作，getState 时实时计算）
    } else if (!valid && isHolding) {
      // 姿势崩溃，停止计时，累加已有时间
      totalElapsed += timestamp - holdStartTime;
      isHolding = false;
      holdStartTime = null;
    }
    // !valid && !isHolding → 无操作
  }

  function getState() {
    return {
      elapsed: totalElapsed + (isHolding && holdStartTime != null ? Date.now() - holdStartTime : 0),
      isHolding,
    };
  }

  // 支持传入外部时间戳的 getState（用于测试）
  function getStateAt(timestamp) {
    return {
      elapsed: totalElapsed + (isHolding && holdStartTime != null ? timestamp - holdStartTime : 0),
      isHolding,
    };
  }

  function reset() {
    totalElapsed = 0;
    isHolding = false;
    holdStartTime = null;
  }

  return { update, getState: getStateAt.bind(null, Date.now()), getState, reset };
}
```

等一下，测试中的 `getState()` 需要在 `update` 后用传入的时间计算。让我修正：上面 `update` 里应该在每次调用时保存 `lastTimestamp`，`getState` 用 `lastTimestamp` 来算 elapsed。重新设计：

```js
// src/detection/zhan-zhuang.js — 站桩计时器（静态姿势检测 + 累计计时）
import { calcAngle } from './pose-detector.js';

const LEFT_HIP = 23;
const LEFT_KNEE = 25;
const LEFT_ANKLE = 27;
const RIGHT_HIP = 24;
const RIGHT_KNEE = 26;
const RIGHT_ANKLE = 28;

const MIN_KNEE_ANGLE = 120;
const MAX_KNEE_ANGLE = 175;

function isValidStance(landmarks) {
  const leftAngle = calcAngle(
    landmarks[LEFT_HIP], landmarks[LEFT_KNEE], landmarks[LEFT_ANKLE]
  );
  const rightAngle = calcAngle(
    landmarks[RIGHT_HIP], landmarks[RIGHT_KNEE], landmarks[RIGHT_ANKLE]
  );
  const avgAngle = (leftAngle + rightAngle) / 2;
  return avgAngle >= MIN_KNEE_ANGLE && avgAngle <= MAX_KNEE_ANGLE;
}

/**
 * 创建站桩计时器
 */
export function createZhanZhuangTimer() {
  let totalElapsed = 0;
  let isHolding = false;
  let holdStartTime = null;
  let lastTimestamp = 0;

  function update(landmarks, timestamp) {
    if (!landmarks) return;
    lastTimestamp = timestamp;

    const valid = isValidStance(landmarks);

    if (valid && !isHolding) {
      isHolding = true;
      holdStartTime = timestamp;
    } else if (!valid && isHolding) {
      // 姿势崩溃，累加时间
      totalElapsed += timestamp - holdStartTime;
      isHolding = false;
      holdStartTime = null;
    }
  }

  function getState() {
    const currentElapsed = isHolding && holdStartTime != null
      ? totalElapsed + (lastTimestamp - holdStartTime)
      : totalElapsed;
    return { elapsed: currentElapsed, isHolding };
  }

  function reset() {
    totalElapsed = 0;
    isHolding = false;
    holdStartTime = null;
    lastTimestamp = 0;
  }

  return { update, getState, reset };
}
```

- [ ] 10.4 运行测试确认通过

```bash
cd fitpet && npx vitest run tests/detection/zhan-zhuang.test.js
```

期望输出：`5 passed`

---

## Task 11: 运动主窗口 UI

**Files:** Modify `fitpet/src/renderer/workout/index.html`, Create `fitpet/src/renderer/workout/workout.js`, Create `fitpet/src/renderer/workout/workout.css`

- [ ] 11.1 实现 `fitpet/src/renderer/workout/workout.css`

```css
/* workout.css — 运动主窗口样式（像素风） */
* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: 'Courier New', monospace;
  background: #1a1a2e;
  color: #e0e0e0;
  display: flex;
  height: 100vh;
}

/* 左侧：摄像头画面 */
.camera-panel {
  flex: 2;
  position: relative;
  background: #000;
}

.camera-panel video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transform: scaleX(-1); /* 镜像 */
}

.camera-panel canvas {
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 100%;
  transform: scaleX(-1);
}

/* 实时计数叠加层 */
.counter-overlay {
  position: absolute;
  bottom: 20px; left: 20px;
  background: rgba(0,0,0,0.7);
  padding: 12px 20px;
  border: 2px solid #00ff88;
  font-size: 32px;
  color: #00ff88;
  image-rendering: pixelated;
}

/* 右侧：运动计划面板 */
.plan-panel {
  flex: 1;
  padding: 20px;
  background: #16213e;
  overflow-y: auto;
  border-left: 2px solid #0f3460;
}

.plan-panel h2 {
  color: #e94560;
  margin-bottom: 16px;
  font-size: 18px;
}

.exercise-item {
  padding: 12px;
  margin-bottom: 8px;
  border: 2px solid #0f3460;
  background: #1a1a2e;
  cursor: pointer;
  transition: border-color 0.2s;
}

.exercise-item.active {
  border-color: #e94560;
}

.exercise-item.completed {
  border-color: #00ff88;
  opacity: 0.7;
}

.exercise-item .name {
  font-size: 16px;
  font-weight: bold;
}

.exercise-item .progress {
  font-size: 14px;
  color: #888;
  margin-top: 4px;
}

/* 凯格尔定时器 */
.kegel-timer {
  text-align: center;
  padding: 20px;
}

.kegel-timer .countdown {
  font-size: 48px;
  color: #e94560;
}

.kegel-timer button {
  margin-top: 12px;
  padding: 10px 24px;
  background: #e94560;
  color: white;
  border: none;
  font-family: 'Courier New', monospace;
  font-size: 16px;
  cursor: pointer;
}

.kegel-timer button:hover {
  background: #ff6b81;
}
```

- [ ] 11.2 实现 `fitpet/src/renderer/workout/index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>FitPet 运动</title>
  <link rel="stylesheet" href="workout.css">
</head>
<body>
  <!-- 左侧：摄像头 + 骨架叠加 -->
  <div class="camera-panel">
    <video id="camera" autoplay playsinline></video>
    <canvas id="skeleton"></canvas>
    <div class="counter-overlay" id="counter">准备开始</div>
  </div>

  <!-- 右侧：运动计划列表 -->
  <div class="plan-panel">
    <h2>📋 今日计划</h2>
    <div id="plan-list"></div>

    <!-- 凯格尔专用面板（默认隐藏） -->
    <div class="kegel-timer" id="kegel-panel" style="display:none;">
      <p>凯格尔训练</p>
      <div class="countdown" id="kegel-countdown">10</div>
      <button id="kegel-start-btn">开始一组</button>
    </div>
  </div>

  <script src="workout.js" type="module"></script>
</body>
</html>
```

- [ ] 11.3 实现 `fitpet/src/renderer/workout/workout.js`

```js
// src/renderer/workout/workout.js — 运动主窗口逻辑
// 在渲染进程中运行，通过 preload 暴露的 window.fitpet 与主进程通信

import { initPoseDetector, detectPose } from '../../detection/pose-detector.js';
import { createPushupCounter } from '../../detection/pushup.js';
import { createSquatCounter } from '../../detection/squat.js';
import { createSitupCounter } from '../../detection/situp.js';
import { createZhanZhuangTimer } from '../../detection/zhan-zhuang.js';

// 运动检测器映射
const DETECTORS = {
  pushup: createPushupCounter,
  squat: createSquatCounter,
  situp: createSitupCounter,
  zhan_zhuang: createZhanZhuangTimer,
};

// 运动中文名
const EXERCISE_NAMES = {
  pushup: '俯卧撑',
  squat: '深蹲',
  zhan_zhuang: '站桩',
  situp: '仰卧起坐',
  kegel: '凯格尔',
};

let currentExercise = null;   // 当前选中的运动项目
let currentDetector = null;   // 当前检测器实例
let plans = [];               // 运动计划列表
let sessions = {};            // { exercise: { completedSets, totalSets } }
let animFrameId = null;

// ===== 初始化 =====

async function init() {
  // 加载运动计划（通过 IPC 从主进程获取，或直接本地加载）
  // 这里用硬编码默认计划，后续 Task 13 接入 DB
  plans = [
    { exercise: 'pushup', sets: 3, reps: 15, duration_seconds: null },
    { exercise: 'squat', sets: 3, reps: 20, duration_seconds: null },
    { exercise: 'zhan_zhuang', sets: 1, reps: null, duration_seconds: 300 },
    { exercise: 'situp', sets: 3, reps: 20, duration_seconds: null },
    { exercise: 'kegel', sets: 3, reps: null, duration_seconds: 10 },
  ];

  // 初始化会话状态
  plans.forEach(p => {
    sessions[p.exercise] = { completedSets: 0, totalSets: p.sets };
  });

  renderPlanList();
  await startCamera();
  await initPoseDetector();
}

// ===== 摄像头 =====

async function startCamera() {
  const video = document.getElementById('camera');
  const stream = await navigator.mediaDevices.getUserMedia({
    video: { width: 640, height: 480, facingMode: 'user' },
  });
  video.srcObject = stream;
  await video.play();
}

// ===== 检测循环 =====

function startDetectionLoop() {
  const video = document.getElementById('camera');
  const canvas = document.getElementById('skeleton');
  const ctx = canvas.getContext('2d');
  const counterEl = document.getElementById('counter');

  canvas.width = video.videoWidth;
  canvas.height = video.videoHeight;

  function loop() {
    const landmarks = detectPose(video, performance.now());

    // 清除骨架画布
    ctx.clearRect(0, 0, canvas.width, canvas.height);

    if (landmarks) {
      // 绘制骨架关键点（简化：画圆点）
      ctx.fillStyle = '#00ff88';
      landmarks.forEach(lm => {
        if (lm.visibility > 0.5) {
          ctx.beginPath();
          ctx.arc(lm.x * canvas.width, lm.y * canvas.height, 4, 0, 2 * Math.PI);
          ctx.fill();
        }
      });

      // 更新检测器
      if (currentDetector) {
        if (currentExercise === 'zhan_zhuang') {
          currentDetector.update(landmarks, performance.now());
          const state = currentDetector.getState();
          const secs = Math.floor(state.elapsed / 1000);
          counterEl.textContent = `站桩 ${Math.floor(secs / 60)}:${String(secs % 60).padStart(2, '0')}`;
        } else {
          currentDetector.update(landmarks);
          const state = currentDetector.getState();
          counterEl.textContent = `${EXERCISE_NAMES[currentExercise]} × ${state.count}`;
        }
      }
    }

    animFrameId = requestAnimationFrame(loop);
  }

  loop();
}

// ===== 计划列表渲染 =====

function renderPlanList() {
  const listEl = document.getElementById('plan-list');
  listEl.innerHTML = '';

  plans.forEach(plan => {
    const div = document.createElement('div');
    div.className = 'exercise-item';
    if (sessions[plan.exercise].completedSets >= plan.sets) {
      div.classList.add('completed');
    }
    if (currentExercise === plan.exercise) {
      div.classList.add('active');
    }

    const target = plan.reps
      ? `${plan.sets}组 × ${plan.reps}个`
      : `${plan.sets}组 × ${plan.duration_seconds}秒`;

    div.innerHTML = `
      <div class="name">${EXERCISE_NAMES[plan.exercise]}</div>
      <div class="progress">${sessions[plan.exercise].completedSets}/${plan.sets} 组 | 目标：${target}</div>
    `;

    div.addEventListener('click', () => selectExercise(plan.exercise));
    listEl.appendChild(div);
  });
}

// ===== 选择运动项目 =====

function selectExercise(exercise) {
  currentExercise = exercise;

  if (exercise === 'kegel') {
    // 凯格尔用手动计时
    document.getElementById('kegel-panel').style.display = 'block';
    currentDetector = null;
  } else {
    document.getElementById('kegel-panel').style.display = 'none';
    const factory = DETECTORS[exercise];
    if (factory) {
      currentDetector = factory();
      startDetectionLoop();
    }
  }

  renderPlanList();
}

// ===== 凯格尔定时器 =====

document.getElementById('kegel-start-btn').addEventListener('click', () => {
  let remaining = 10;
  const countdownEl = document.getElementById('kegel-countdown');
  const btn = document.getElementById('kegel-start-btn');
  btn.disabled = true;

  const timer = setInterval(() => {
    remaining--;
    countdownEl.textContent = remaining;

    if (remaining <= 0) {
      clearInterval(timer);
      btn.disabled = false;
      countdownEl.textContent = '✅';
      sessions.kegel.completedSets++;
      renderPlanList();
      checkAllCompleted();

      // 1秒后重置显示
      setTimeout(() => { countdownEl.textContent = '10'; }, 1000);
    }
  }, 1000);
});

// ===== 完成检查 =====

function checkAllCompleted() {
  const allDone = plans.every(p => sessions[p.exercise].completedSets >= p.sets);
  if (allDone) {
    // 通知主进程
    const today = new Date().toISOString().slice(0, 10);
    window.fitpet.workoutCompleted({
      date: today,
      sessions: Object.entries(sessions).map(([exercise, s]) => ({
        exercise,
        completed_sets: s.completedSets,
        total_sets: s.totalSets,
      })),
    });
  }
}

// 启动
init().catch(err => console.error('初始化失败:', err));
```

- [ ] 11.4 手动验证：运行应用，检查摄像头画面、骨架叠加、计划列表显示

```bash
cd fitpet && npx electron .
```

期望：主窗口显示摄像头画面和右侧计划列表，点击项目可切换当前运动。

---

## Task 12: 宠物悬浮窗

**Files:** Modify `fitpet/src/renderer/pet-overlay/index.html`, Create `fitpet/src/renderer/pet-overlay/pet.js`, Create `fitpet/src/renderer/pet-overlay/pet.css`

- [ ] 12.1 实现 `fitpet/src/renderer/pet-overlay/pet.css`

```css
/* pet.css — 宠物悬浮窗样式 */
* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  background: transparent;
  font-family: 'Courier New', monospace;
  overflow: hidden;
  user-select: none;
}

.pet-container {
  width: 200px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 像素宠物（CSS 占位绘制） */
.pet-sprite {
  width: 80px;
  height: 80px;
  image-rendering: pixelated;
  cursor: pointer;
}

/* CSS 绘制的像素蛋（第一境界占位） */
.pet-sprite.egg {
  background: #ffd93d;
  border-radius: 40% 40% 50% 50%;
  border: 3px solid #e8a317;
  position: relative;
  animation: bob 2s ease-in-out infinite;
}

.pet-sprite.egg::after {
  content: '';
  position: absolute;
  top: 30%; left: 25%;
  width: 50%; height: 30%;
  background: #fff;
  border-radius: 50%;
  opacity: 0.3;
}

/* 活跃动画 */
.pet-sprite.active {
  animation: bounce 0.5s ease-in-out infinite;
}

/* 萎靡动画 */
.pet-sprite.tired {
  animation: droop 3s ease-in-out infinite;
  opacity: 0.6;
}

@keyframes bob {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-5px); }
}

@keyframes bounce {
  0%, 100% { transform: translateY(0) scale(1); }
  50% { transform: translateY(-12px) scale(1.05); }
}

@keyframes droop {
  0%, 100% { transform: translateY(0) rotate(0); }
  50% { transform: translateY(3px) rotate(-5deg); }
}

/* 状态信息 */
.pet-info {
  text-align: center;
  color: #fff;
  text-shadow: 1px 1px 2px rgba(0,0,0,0.8);
  margin-top: 4px;
  font-size: 11px;
}

/* 进度条 */
.progress-bar {
  width: 80px;
  height: 6px;
  background: rgba(255,255,255,0.2);
  border-radius: 3px;
  margin-top: 4px;
  overflow: hidden;
}

.progress-bar .fill {
  height: 100%;
  background: #00ff88;
  transition: width 0.5s;
}

/* 展开面板 */
.expand-panel {
  display: none;
  background: rgba(22, 33, 62, 0.95);
  border: 1px solid #0f3460;
  border-radius: 8px;
  padding: 12px;
  margin-top: 8px;
  width: 180px;
  color: #e0e0e0;
  font-size: 12px;
}

.expand-panel.visible {
  display: block;
}

.expand-panel .detail-item {
  margin-bottom: 6px;
}

.expand-panel button {
  width: 100%;
  padding: 8px;
  margin-top: 8px;
  background: #e94560;
  color: white;
  border: none;
  font-family: 'Courier New', monospace;
  cursor: pointer;
  border-radius: 4px;
}

.expand-panel button:hover {
  background: #ff6b81;
}
```

- [ ] 12.2 实现 `fitpet/src/renderer/pet-overlay/index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>FitPet</title>
  <link rel="stylesheet" href="pet.css">
</head>
<body>
  <div class="pet-container" id="pet-container">
    <!-- 宠物 Sprite -->
    <div class="pet-sprite egg" id="pet-sprite"></div>

    <!-- 状态信息 -->
    <div class="pet-info">
      <div id="realm-level">第一境界 · Lv.1</div>
      <div class="progress-bar">
        <div class="fill" id="progress-fill" style="width: 0%"></div>
      </div>
    </div>

    <!-- 展开面板 -->
    <div class="expand-panel" id="expand-panel">
      <div class="detail-item">📅 连续打卡：<span id="streak">0</span> 天</div>
      <div class="detail-item">📊 今日完成：<span id="today-rate">0%</span></div>
      <div id="exercise-details"></div>
      <button id="start-workout-btn">🏋️ 开始运动</button>
    </div>
  </div>

  <script src="pet.js" type="module"></script>
</body>
</html>
```

- [ ] 12.3 实现 `fitpet/src/renderer/pet-overlay/pet.js`

```js
// src/renderer/pet-overlay/pet.js — 宠物悬浮窗逻辑

const petSprite = document.getElementById('pet-sprite');
const expandPanel = document.getElementById('expand-panel');
const realmLevelEl = document.getElementById('realm-level');
const progressFill = document.getElementById('progress-fill');
const streakEl = document.getElementById('streak');
const todayRateEl = document.getElementById('today-rate');
const startBtn = document.getElementById('start-workout-btn');

let expanded = false;

// 境界名称
const REALM_NAMES = ['第一境界', '第二境界', '第三境界'];

function getRealmName(realm) {
  return REALM_NAMES[realm - 1] || `第${realm}境界`;
}

// ===== 点击宠物切换展开/收起 =====
petSprite.addEventListener('mouseenter', () => {
  // 鼠标进入宠物区域，通知主进程取消穿透
  window.fitpet.setIgnoreMouse(false);
});

petSprite.addEventListener('mouseleave', () => {
  if (!expanded) {
    // 鼠标离开且未展开，恢复穿透
    window.fitpet.setIgnoreMouse(true);
  }
});

petSprite.addEventListener('click', () => {
  expanded = !expanded;
  expandPanel.classList.toggle('visible', expanded);
  window.fitpet.setIgnoreMouse(!expanded);
});

// 展开面板区域也需要保持非穿透
expandPanel.addEventListener('mouseenter', () => {
  window.fitpet.setIgnoreMouse(false);
});

expandPanel.addEventListener('mouseleave', () => {
  if (!expanded) {
    window.fitpet.setIgnoreMouse(true);
  }
});

// ===== 开始运动按钮 =====
startBtn.addEventListener('click', () => {
  window.fitpet.showWorkout();
});

// ===== 接收宠物状态更新 =====
window.fitpet.onPetUpdate((data) => {
  // data: { realm, level, levelInRealm, completionRate, streak, mood, exercises }

  // 更新境界/级别显示
  realmLevelEl.textContent = `${getRealmName(data.realm)} · Lv.${data.levelInRealm}`;

  // 更新进度条
  const pct = Math.round((data.completionRate || 0) * 100);
  progressFill.style.width = `${pct}%`;
  todayRateEl.textContent = `${pct}%`;

  // 更新连续打卡
  streakEl.textContent = data.streak || 0;

  // 更新宠物心情动画
  petSprite.className = 'pet-sprite egg'; // 基础类
  if (data.mood === 'active') {
    petSprite.classList.add('active');
  } else if (data.mood === 'tired') {
    petSprite.classList.add('tired');
  }

  // 更新运动详情
  if (data.exercises) {
    const detailsEl = document.getElementById('exercise-details');
    detailsEl.innerHTML = data.exercises.map(e =>
      `<div class="detail-item">${e.done ? '✅' : '⬜'} ${e.name}: ${e.completedSets}/${e.totalSets}组</div>`
    ).join('');
  }
});
```

- [ ] 12.4 手动验证

```bash
cd fitpet && npx electron .
```

期望：右下角出现黄色像素蛋悬浮窗，点击展开显示详情面板，点击"开始运动"弹出主窗口。非宠物区域鼠标可穿透到桌面。

---

## Task 13: IPC 串联（运动完成 → 宠物更新）

**Files:** Modify `fitpet/main.js`, Modify `fitpet/preload.js`

- [ ] 13.1 在 `main.js` 中引入 DB 模块，处理完整的运动完成流程

```js
// 在 main.js 顶部新增引入
const { createDatabase } = require('./src/db/schema.js');
const { getWorkoutPlans, saveSession, getCompletionRate } = require('./src/db/workout.js');
const { getPetState, updatePetState, saveDailySummary, getConsecutiveHighDays } = require('./src/db/pet.js');
const { calculateNewLevel, getPetMood, getLevelInRealm, getRealmInfo } = require('./src/pet/growth.js');
const path = require('path');
const { app } = require('electron');

// 初始化数据库（存在用户数据目录）
const dbPath = path.join(app.getPath('userData'), 'fitpet.db');
let db;

// 在 app.whenReady 中初始化 DB
// （修改已有的 app.whenReady 回调）
app.whenReady().then(() => {
  db = createDatabase(dbPath);
  createPetWindow();
  createWorkoutWindow();

  // 启动时发送宠物状态到悬浮窗
  setTimeout(() => sendPetStatus(), 1000);
});

// 新增：处理运动完成事件
ipcMain.on('workout-completed', (_event, data) => {
  const { date, sessions } = data;

  // 保存每项运动记录
  for (const s of sessions) {
    saveSession(db, {
      date,
      exercise: s.exercise,
      completed_sets: s.completed_sets,
      total_sets: s.total_sets,
    });
  }

  // 计算完成率
  const completionRate = getCompletionRate(db, date);

  // 获取宠物当前状态
  const petState = getPetState(db);

  // 计算连续高完成率天数
  const consecutiveHighDays = getConsecutiveHighDays(db, date);

  // 计算新级别
  const { level: newLevel, realm: newRealm } = calculateNewLevel(
    petState.current_realm,
    petState.current_level,
    completionRate,
    consecutiveHighDays
  );

  // 计算连续打卡天数
  const yesterday = new Date(date);
  yesterday.setDate(yesterday.getDate() - 1);
  const yesterdayStr = yesterday.toISOString().slice(0, 10);
  const streak = petState.last_active_date === yesterdayStr
    ? petState.streak_days + 1
    : (petState.last_active_date === date ? petState.streak_days : 1);

  // 更新宠物状态
  updatePetState(db, {
    current_realm: newRealm,
    current_level: newLevel,
    streak_days: streak,
    last_active_date: date,
  });

  // 保存每日汇总
  saveDailySummary(db, date, completionRate, streak);

  // 通知悬浮窗
  sendPetStatus();
});

// 发送宠物状态到悬浮窗
function sendPetStatus() {
  if (!petWindow || !db) return;

  const petState = getPetState(db);
  const today = new Date().toISOString().slice(0, 10);
  const completionRate = getCompletionRate(db, today);
  const mood = getPetMood(completionRate);
  const plans = getWorkoutPlans(db);

  petWindow.webContents.send('pet-update', {
    realm: petState.current_realm,
    level: petState.current_level,
    levelInRealm: getLevelInRealm(petState.current_level),
    completionRate,
    streak: petState.streak_days,
    mood,
    exercises: plans.map(p => ({
      name: p.exercise,
      completedSets: 0, // 从 daily_sessions 查实际值（简化）
      totalSets: p.sets,
      done: false,
    })),
  });
}

// 新增 IPC：渲染进程请求运动计划
ipcMain.handle('get-workout-plans', () => {
  return getWorkoutPlans(db);
});
```

- [ ] 13.2 更新 `preload.js` 增加 `invoke` 支持

```js
// 在 preload.js 的 contextBridge.exposeInMainWorld 中新增：
getWorkoutPlans: () => ipcRenderer.invoke('get-workout-plans'),
```

- [ ] 13.3 端到端手动测试

```bash
cd fitpet && npx electron .
```

验证流程：
1. 悬浮窗显示"第一境界 · Lv.1"
2. 点击宠物展开面板，点击"开始运动"
3. 主窗口弹出，显示计划列表
4. 对着摄像头做几个俯卧撑，观察计数
5. 手动完成所有项目后，悬浮窗进度条更新
6. 关闭再打开应用，数据持久化

期望：完整运动流程跑通，宠物级别根据完成率更新。

---

## 附录：vitest 配置

在 `package.json` 同级创建 `vitest.config.js`（Task 1 中一起创建）：

```js
// vitest.config.js
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
  },
});
```

## 附录：Sprite 占位资源

`fitpet/src/assets/sprites/` 目录下放一个 `README.md`：

```markdown
# Sprite 资源说明
当前版本使用 CSS 绘制占位宠物形象。
后续可替换为真实像素美术 Sprite Sheet（PNG 格式，建议 16×16 或 32×32 单帧）。
```
